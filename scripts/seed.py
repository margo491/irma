"""Начальное наполнение меню тестовыми данными."""
import sys
import os

sys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))

import psycopg2
from dotenv import load_dotenv

load_dotenv()

# Берём sync-URL из DATABASE_URL, заменяя asyncpg на psycopg2
db_url = os.getenv("DATABASE_URL", "").replace("postgresql+asyncpg", "postgresql")

conn = psycopg2.connect(db_url)
conn.autocommit = False
cur = conn.cursor()

CATEGORIES = [
    (1, "Напитки",   1),
    (2, "Салаты",    2),
    (3, "Завтраки",  3),
    (4, "Пицца",     4),
    (5, "Остальное", 5),
]

ITEMS = [
    # (category_id, name, description, price)
    # Напитки
    (1, "Кофе эспрессо",      "Классический двойной эспрессо",              120),
    (1, "Капучино",           "Эспрессо с молочной пенкой",                 180),
    (1, "Латте",              "Мягкий кофе с большим количеством молока",   200),
    (1, "Чай чёрный",         "Цейлонский листовой чай",                    100),
    (1, "Апельсиновый сок",   "Свежевыжатый сок",                          220),

    # Салаты
    (2, "Цезарь с курицей",   "Салат с куриной грудкой, сухариками и пармезаном", 380),
    (2, "Греческий",          "Помидоры, огурцы, маслины, фета",                  320),
    (2, "Оливье",             "Классический русский салат",                        290),
    (2, "Тёплый с лососем",   "Лосось на гриле с рукколой и авокадо",             450),

    # Завтраки
    (3, "Яичница с беконом",  "Два яйца, хрустящий бекон, тост",          280),
    (3, "Овсяная каша",       "На молоке с ягодами и мёдом",              220),
    (3, "Панкейки",           "Стопка из 4 штук с кленовым сиропом",      310),
    (3, "Авокадо-тост",       "Ржаной хлеб, авокадо, яйцо пашот",        350),

    # Пицца
    (4, "Маргарита",          "Томатный соус, моцарелла, базилик",        490),
    (4, "Пепперони",          "Томатный соус, моцарелла, пепперони",      560),
    (4, "Четыре сыра",        "Моцарелла, горгонзола, пармезан, чеддер",  590),
    (4, "Гавайская",          "Томатный соус, курица, ананас",            520),

    # Остальное
    (5, "Борщ",               "Традиционный украинский борщ со сметаной", 290),
    (5, "Паста карбонара",    "Спагетти, бекон, яйцо, пармезан",         420),
    (5, "Бургер классик",     "Говяжья котлета, салат, томат, соус",      480),
    (5, "Чизкейк",            "Нью-йоркский чизкейк с ягодным соусом",   280),
]

try:
    # Категории
    cur.execute("DELETE FROM menu_items")
    cur.execute("DELETE FROM menu_categories")

    cur.executemany(
        "INSERT INTO menu_categories (id, name, sort_order) VALUES (%s, %s, %s)",
        CATEGORIES,
    )

    cur.executemany(
        """INSERT INTO menu_items (category_id, name, description, price, is_available)
           VALUES (%s, %s, %s, %s, true)""",
        ITEMS,
    )

    conn.commit()
    print(f"Готово: {len(CATEGORIES)} категорий, {len(ITEMS)} позиций меню")

except Exception as e:
    conn.rollback()
    print(f"Ошибка: {e}")
    sys.exit(1)
finally:
    cur.close()
    conn.close()
