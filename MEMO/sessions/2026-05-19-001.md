> Мигрировано из .claude/history/DESKTOP-FLG48TD_Маргарита_2026-05-19_00-10.md (agent_id: horar-astrology-claude-v1)

# Сессия 2026-05-19

## Задачи сессии

### 1. Миграция horar на новую структуру VDS
**Что сделано:**
- Добавлен `docker-compose.test.yml` — test-контур с изолированной сетью `10.11.0.0/24`, портами 8010/3010, `env_file: .env.test`
- Полностью переписан `nginx/templates/app.conf.template` — 3 новых субдомена:
  - `horar.bot-ktu.online` — frontend + API (для пользователей)
  - `horar-api.bot-ktu.online` — прямой backend (боты, интеграции)
  - `horar-admin.bot-ktu.online` — admin панель
  - Старые домены `bot-ktu.online`, `astra.bot-ktu.online` → 301-редиректы (с сохранением `/bot/` без редиректа для Telegram)
- Получен SAN SSL-сертификат через certbot для всех 3 новых субдоменов
- Перенесён каталог `/srv/horar-astrology` → `/srv/projects/horar` на VDS
- Созданы `.env` и `.env.prod` в новом каталоге с `COMPOSE_PROJECT_NAME=horar-astrology` (сохранение postgres volumes)
- Установлен Telegram webhook на `https://horar-api.bot-ktu.online/bot/webhook`
- Обновлён GitHub Secret `VPS_DEPLOY_PATH` → `/srv/projects/horar`
- CI задеплоил в новый путь, smoke-тесты прошли
- Удалён старый каталог `/srv/horar-astrology`
- Обновлены: `CLAUDE.md`, `.env.example`, `.github/workflows/deploy.yml`, `.claude/plans/`

**Грабли:**
- nginx краш-луп после деплоя нового шаблона — SSL cert `live/horar.bot-ktu.online/` ещё не существовал. Фикс: временный откат шаблона через `git show HEAD~1 > template`, получение cert, возврат нового шаблона
- Telegram webhook удалён случайно — `${token}` не раскрылся внутри SSH-команды (single-quote в PowerShell). Webhook указал на пустой URL. Фикс: `Invoke-WebRequest` напрямую с local machine
- CI продолжал деплоить в `/srv/horar-astrology` пока GitHub Secret не обновили — поэтому контейнеры вернулись на старый путь после первого CI run
- PowerShell `&&` не работает — использовали Bash tool или `;` для ssh-команд

## Коммиты сессии

| Hash | Сообщение |
|---|---|
| `a5efeb0` | feat(infra): add test contour, new 3-domain nginx config for horar migration |
| `be28d8b` | fix(nginx): proxy bot webhooks on old domain without redirect |
| `83386e4` | chore(infra): update deploy path and smoke test domains for horar migration |
| `33a594a` | ci: trigger deploy to /srv/projects/horar |
