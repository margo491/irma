> Мигрировано из .claude/history/DESKTOP-FLG48TD_Маргарита_2026-05-19_10-00.md (agent_id: horar-astrology-claude-v1)

# Сессия 2026-05-19

## Задачи сессии

### 1. Smoke-check всех продакшн-субдоменов

**Что сделано:**
- Создан `scripts/smoke-check.ps1` — PowerShell 5.1 скрипт, проверяет 7 субдоменов по цепочке DNS → TLS → HTTP
- Проверяет: DNS (A или AAAA), TLS (SslPolicyErrors через C# `TlsCapture` + ServicePointManager), статус, Content-Type (200) или Location (301)
- Создан `tools/dev-index.html` — локальная dark-theme страница со ссылками на все сервисы, открывать через `file://`, не деплоится
- Добавлен комментарий в `nginx/templates/app.conf.template` над блоком `astra.bot-ktu.online`
- Добавлена цель `make smoke` в `Makefile`
- Все 7 проверок прошли: DNS ✓, TLS 88–90 дней, HTTP статусы и редиректы корректны

**Грабли:**
- `pwsh` не установлен на машине — только `powershell.exe` (Windows PowerShell 5.1); скрипт запускался через `& powershell.exe -File ...`
- `$host` — зарезервированная переменная в PS 5.1; переименовано в `$hostname`
- `[System.Net.Http.HttpClientHandler+ServerCertificateCustomValidationCallback]` недоступен в PS 5.1 — переключились на `ServicePointManager` + `Add-Type` C#-класс `TlsCapture`
- Ручной regex по SAN некорректно репортил NameMismatch для `horar-api` — fixed через `SslPolicyErrors` из .NET callback
- `[Net.Security.RemoteCertificateValidationCallback][TlsCapture]::Callback` — прямой каст PSMethod не работает; fixed через `[Delegate]::CreateDelegate()`

## Коммиты сессии

| Hash | Сообщение |
|---|---|
| `cd08700` | feat(infra): add subdomain smoke-check and dev-index |
