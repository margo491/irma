> Мигрировано из .claude/history/DESKTOP-FLG48TD_Маргарита_2026-05-19_18-00.md (agent_id: horar-astrology-claude-v1)

# Сессия 2026-05-19

## Задачи сессии

### 1. Три контура prod/test/dev одновременно

**Что сделано:**
- Изучены все планы: `vds-standard-v2.md` уже описывал три контура, `docker-compose.test.yml` и `docker-compose.dev.yml` уже существовали — нужно было только соединить в живую инфраструктуру
- Написан новый план в `.claude/plans/stateful-cuddling-boot.md` (заменил предыдущий план аудита)
- `docker-compose.prod.yml` — добавлен `extra_hosts: host.docker.internal:host-gateway` к nginx: позволяет nginx-контейнеру проксировать на host-порты 8010/3010 где живут test-контейнеры
- `nginx/templates/app.conf.template` — добавлены 6 server-блоков (80+443 × 3): `test.horar.bot-ktu.online`, `test-admin.bot-ktu.online`, `test-api.bot-ktu.online` → проксируют на `host.docker.internal:3010/8010`
- `.github/workflows/deploy-test.yml` — новый CI: push в `dev` → typecheck → SSH → `COMPOSE_PROJECT_NAME=horar-test docker compose up -d --build backend frontend scheduler` → smoke `/api/health`
- `.github/workflows/deploy.yml` — исправлен: добавлен `git checkout master` перед `git pull` (иначе после test-деплоя HEAD мог остаться на dev)
- `tools/dev-index.html` — переименован в "Prod & Test", добавлена секция Тест с 4 ссылками

**Архитектура:**
- prod: master → `horar.bot-ktu.online`, `horar-admin.*`, `horar-api.*`
- test: dev → `test.horar.bot-ktu.online`, `test-admin.*`, `test-api.*` (порты 8010/3010)
- dev: локальная машина → `localhost:3000/8000` (docker-compose + dev оверлей)

**Что осталось сделать вручную на VDS (разовые шаги):**
1. Создать `.env.test` (cp .env + изменить DB, ENVIRONMENT, BOT TOKEN, YooKassa, URLs)
2. Создать @HorarTestBot в BotFather → вставить токен в `.env.test`
3. Выпустить SSL для новых субдоменов через certbot (expand cert)
4. Первый запуск test-контура: `COMPOSE_PROJECT_NAME=horar-test docker compose ... up -d --build`
5. Зарегистрировать webhook тест-бота на `test-api.bot-ktu.online/bot/webhook`
6. Переключить VDS обратно на master

## Коммиты сессии

| Hash | Сообщение |
|---|---|
| `647d9ec` | feat(infra): add test contour — nginx, CI, compose |
