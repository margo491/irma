> Мигрировано из .claude/history/DESKTOP-FLG48TD_Маргарита_2026-05-19_14-00.md (agent_id: horar-astrology-claude-v1)

# Сессия 2026-05-19

## Задачи сессии

### 1. Аудит трёх сайтов на dev-ветке

**Что сделано:**
- Исследована структура проекта: три «сайта» — роуты одного Next.js-приложения (`/cabinet`, `/astrologer`, `/admin`)
- Через `git ls-tree origin/dev` подтверждено: на dev отсутствует `frontend/src/app/astrologer/` и `backend/app/routers/astrologer.py` — B2B по текущей проверке не найден
- Выяснено: `astra.bot-ktu.online` — это 301 redirect на `horar-admin`, а не B2B-портал (подтверждено анализом nginx-конфига)

**Грабли:**
- Ожидали найти отдельные директории `b2c/`, `b2b/` — их нет, структура роутная
- `astra.bot-ktu.online` выглядел как B2B, оказался алиасом admin

### 2. Исправление подтверждённых ошибок

**Что сделано:**
- `frontend/src/middleware.ts:4` — добавлен fail-fast guard: если `SECRET_KEY` не задан, Next.js падает с явной ошибкой вместо тихого провала JWT-верификации
- `frontend/src/app/cabinet/page.tsx:70` — `Promise.all` → `Promise.allSettled`: подписка и вопросы теперь загружаются независимо; новый пользователь без подписки видит историю вопросов и активный CTA
- `frontend/src/app/admin/layout.tsx:9` — `API_URL` → `BACKEND_URL`, согласовано с `next.config.js` и docker-compose

**Грабли:**
- При замене Promise.all случайно оставил `try {} catch {} finally {` — пришлось чистить отдельным Edit

### 3. Обновление dev-ветки и деплой

**Что сделано:**
- `git merge master --ff-only` на dev — fast-forward, все 93 файла подтянуты включая B2B
- Push origin/dev и origin/master через `GIT_SSH_COMMAND` с ключом из `.env`
- Ручной rebuild frontend на VDS (`docker compose build --no-cache frontend && up -d`)
- Smoke-check: все 5 точек отвечают 200 OK (horar, horar-admin, /cabinet, /astrologer, /admin)

**Грабли:**
- `git checkout dev` упал из-за незакоммиченного `history.md` — пришлось stash/pop
- SSH для git push не работал по умолчанию (нет ~/.ssh/config) — нужен явный `GIT_SSH_COMMAND`
- Кириллица в пути пользователя вызывала warning known_hosts — некритично, push прошёл

## Коммиты сессии

| Hash | Сообщение |
|---|---|
| `b12dc95` | fix(frontend): audit-driven fixes for all three sites |
