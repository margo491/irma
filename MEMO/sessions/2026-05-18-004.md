> Мигрировано из .claude/history/DESKTOP-FLG48TD_Маргарита_2026-05-18_18-00.md (agent_id: horar-astrology-claude-v1)

# Сессия 2026-05-18

## Задачи сессии

### 1. SSH-доступ для второго пользователя
**Что сделано:** Добавили SSH-ключ `C:\Users\Маргарита\.ssh\id_ed25519` на VDS. Убрали хардкод пути к ключу из CLAUDE.md, вынесли в `SSH_KEY_PATH` в `.env`. Обновили `.env.example`.
**Грабли:** В `.env` оказался дубль `SSH_KEY_PATH` — удалили заглушку.

### 2. Сверка плана с состоянием VDS
**Что сделано:** Опросили VDS через SSH: docker ps, git log, миграции, структура backend/frontend/bot/nginx. Итог — проект опережает план: фазы 1–4 выполнены, E2E тестов нет. Обновили CLAUDE.md.

### 3. Стандарт VDS ver 2 и планы реорганизации
**Что сделано:** Создали три файла в `.claude/plans/`:
- `vds-standard-v2.md` — стандарт под два проекта (horar + ktu), домен `bot-ktu.online`, среды dev/test/prod
- `horar-migration-plan.md` — план переноса `/srv/horar-astrology` → `/srv/projects/horar` с test-контуром
- `ktu-setup-plan.md` — план создания нового проекта ktu в отдельном репо

**Грабли:** Первая попытка SSH с heredoc в PowerShell провалилась — heredoc некорректно передаётся. Пришлось дробить на отдельные команды.

### 4. Доработка планов по ревью Перплексити
**Что сделано:**
- Добавили `COMPOSE_PROJECT_NAME=horar-test` для изоляции test-контура
- Заменили `redis DB 1` на отдельный `redis-test` контейнер
- Убрали ссылку на `ops-notes.md`, вписали конкретную команду `docker compose restart nginx`
- Убрали преждевременный поддомен `test-app.horar.*`
- В ktu-плане отложили фиксацию субдоменов до определения стека

## Коммиты сессии

| Hash | Сообщение |
|---|---|
| `a7028dd` | docs: update current phase status and SSH key path handling |
| `b191860` | docs(vds): add VDS standard ver 2 (horar + ktu, dev/test/prod) |
| `6cc4304` | docs(vds): add horar migration plan to /srv/projects/horar with test/prod envs |
| `a59df70` | docs(vds): add ktu project setup plan under /srv/projects/ktu |
| `194db95` | docs(vds): strengthen test isolation in horar migration plan per review |
| `661c960` | docs(vds): finalize horar migration plan — concrete nginx restart, unambiguous test service names |
